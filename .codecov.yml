comment:
  layout: "reach, diff, flags, files, footer"
  behavior: default
  require_changes: no

coverage:
  status:
    project:
      default: off
      main:
        target: 80%
        flags: main

flags:
  main:
    paths:
      - Sources/

