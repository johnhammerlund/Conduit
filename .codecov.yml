comment:
  layout: "reach, diff, flags, files, footer"
  behavior: default
  require_changes: no

coverage:
  status:
    patch:
      default:
        target: 79%
    project:
      default: off
      main:
        target: 80%
        flags: main

flags:
  main:
    paths:
      - Sources/

ignore:
  - "Example/"
  - "Tests/"
  - "TestHosts/"
  - "images/"
  - ".github/"
